export const STORAGE_KEYS = {
  keyboardShortcut: 'content_app_keyboard_shortcut',
  baseFontSize: 'content_app_base_font_size',
  baseWidth: 'content_app_base_width',
  baseHeight: 'content_app_base_height',
} as const

export const VISUAL_CONFIG_KEY = 'visual_config'

export const DEFAULT_KEYBOARD_SHORTCUT = 'I'
export const DEFAULT_BASE_FONT_SIZE = 16
export const DEFAULT_BASE_WIDTH = 400
export const DEFAULT_BASE_HEIGHT = 400
export const KEY_ESCAPE = 'Escape'
export const KEY_ENTER = 'Enter'
export const REGEX_KEYBOARD_SHORTCUT = /^[a-zA-Z]$/
export const KEYBOARD_SHORTCUT_DELAY = 100
