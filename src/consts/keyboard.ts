export const STORAGE_KEYS = {
  keyboardShortcut: 'content_app_keyboard_shortcut',
}

export const DEFAULT_KEYBOARD_SHORTCUT = 'I'
export const KEY_ESCAPE = 'Escape'
export const KEY_ENTER = 'Enter'
export const REGEX_KEYBOARD_SHORTCUT = /^[a-zA-Z]$/
export const KEYBOARD_SHORTCUT_DELAY = 100
export const DEFAULT_INIT_DELAY = 150
